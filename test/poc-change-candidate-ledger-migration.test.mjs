import test from "node:test";import assert from "node:assert/strict";import {readFile} from "node:fs/promises";
test("POC change candidates preserve approval and validation history",async()=>{const migration=await readFile(new URL("../db/migrations/032_poc_change_candidate_ledger.sql",import.meta.url),"utf8");for(const table of ["poc_change_candidates","poc_change_candidate_decisions","poc_change_candidate_validations"])assert.match(migration,new RegExp(table));for(const severity of ["S0","S1","S2","S3","S4"])assert.match(migration,new RegExp(`'${severity}'`));assert.match(migration,/validated','invalidated','inconclusive/);assert.match(migration,/GRANT SELECT,INSERT/);assert.doesNotMatch(migration,/GRANT[^;]*(UPDATE|DELETE)/);assert.equal((migration.match(/FORCE ROW LEVEL SECURITY/g)||[]).length,3);});
