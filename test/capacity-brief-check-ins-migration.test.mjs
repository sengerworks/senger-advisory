import test from "node:test";import assert from "node:assert/strict";import {readFile} from "node:fs/promises";test("check-ins are tenant-isolated attention records",async()=>{const m=await readFile(new URL("../db/migrations/026_capacity_brief_check_ins.sql",import.meta.url),"utf8");assert.match(m,/open','responded','escalated/);assert.match(m,/attention_reason/);assert.match(m,/FORCE ROW LEVEL SECURITY/);assert.doesNotMatch(m,/participant|email|clerk_user/i);});
