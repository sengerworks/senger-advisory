import test from "node:test";import assert from "node:assert/strict";import {readFile} from "node:fs/promises";test("Learning Path is tenant-isolated and keeps completion distinct from capacity evidence",async()=>{const migration=await readFile(new URL("../db/migrations/024_capacity_brief_learning_path.sql",import.meta.url),"utf8");assert.match(migration,/capacity_brief_learning_items/);assert.match(migration,/application_note/);assert.match(migration,/ENABLE ROW LEVEL SECURITY/);assert.match(migration,/FORCE ROW LEVEL SECURITY/);assert.doesNotMatch(migration,/participant|email|clerk_user/i);});
