import test from "node:test";import assert from "node:assert/strict";import {readFile} from "node:fs/promises";test("Brief evidence journal is tenant-isolated and classifies change evidence",async()=>{const migration=await readFile(new URL("../db/migrations/017_capacity_brief_evidence_journal.sql",import.meta.url),"utf8");assert.match(migration,/capacity_brief_evidence_entries/);assert.match(migration,/'delivery', 'operating-change', 'capacity-change'/);assert.match(migration,/ENABLE ROW LEVEL SECURITY/);assert.match(migration,/FORCE ROW LEVEL SECURITY/);assert.doesNotMatch(migration,/participant|email|clerk_user/i);});

