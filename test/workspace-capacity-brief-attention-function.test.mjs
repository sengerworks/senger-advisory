import test from "node:test";import assert from "node:assert/strict";import {createWorkspaceCapacityBriefAttentionHandler} from "../netlify/functions/workspace-capacity-brief-attention.mjs";import {WORKSPACE_ROLES} from "../workspace-authorization.js";const id="22222222-2222-4222-8222-222222222222",auth=role=>async()=>({ok:true,value:{workspaceId:"11111111-1111-4111-8111-111111111111",userId:"u",role}}),req=(method="GET",origin="https://example.com")=>new Request(`https://example.com/api/workspace/capacity-brief-attention?diagnosticId=${id}`,{method,headers:{origin}});test("client roles receive only governed attention items",async()=>{let received;const h=createWorkspaceCapacityBriefAttentionHandler({authenticate:auth(WORKSPACE_ROLES.owner),getAttention:async value=>(received=value,{state:"active",items:[],summary:{total:0,high:0,medium:0}})});const response=await h(req());assert.equal(response.status,200);assert.equal(received.diagnosticId,id);assert.equal((await response.json()).policy.participantContentIncluded,false);});test("participants, foreign origins, and writes cannot access executive attention",async()=>{const h=createWorkspaceCapacityBriefAttentionHandler({authenticate:auth(WORKSPACE_ROLES.participant)});assert.equal((await h(req())).status,403);assert.equal((await h(req("GET","https://attacker.example"))).status,403);assert.equal((await h(req("POST"))).status,405);});
