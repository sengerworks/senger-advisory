import test from "node:test";import assert from "node:assert/strict";import {readFile} from "node:fs/promises";test("POC feedback becomes readable only for governed server aggregation",async()=>{const m=await readFile(new URL("../db/migrations/030_poc_scorecard_read_boundary.sql",import.meta.url),"utf8");assert.match(m,/GRANT SELECT ON app_operations.poc_checkpoint_feedback/);assert.doesNotMatch(m,/PUBLIC|participant/i);});
