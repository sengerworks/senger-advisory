import test from "node:test";import assert from "node:assert/strict";import {readFile} from "node:fs/promises";test("Action Path is tenant-isolated and separates activity from evidence",async()=>{const m=await readFile(new URL("../db/migrations/025_capacity_brief_action_path.sql",import.meta.url),"utf8");assert.match(m,/phase_days IN \(30,60,90\)/);assert.match(m,/decision_gate/);assert.match(m,/evidence_prompt/);assert.match(m,/FORCE ROW LEVEL SECURITY/);assert.doesNotMatch(m,/participant|email|clerk_user/i);});
