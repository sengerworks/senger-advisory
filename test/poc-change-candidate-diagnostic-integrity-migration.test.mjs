import test from "node:test";import assert from "node:assert/strict";import {readFile} from "node:fs/promises";
test("change decisions and validations cannot cross diagnostic boundaries",async()=>{const migration=await readFile(new URL("../db/migrations/033_poc_change_candidate_diagnostic_integrity.sql",import.meta.url),"utf8");assert.match(migration,/UNIQUE \(workspace_id,diagnostic_id,id\)/);assert.equal((migration.match(/FOREIGN KEY \(workspace_id,diagnostic_id,candidate_id\)/g)||[]).length,2);assert.equal((migration.match(/REFERENCES app_operations\.poc_change_candidates\(workspace_id,diagnostic_id,id\)/g)||[]).length,2);});
