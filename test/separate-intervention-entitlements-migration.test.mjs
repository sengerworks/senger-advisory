import test from "node:test";import assert from "node:assert/strict";import {readFile} from "node:fs/promises";const sql=await readFile(new URL("../db/migrations/023_separate_intervention_entitlements.sql",import.meta.url),"utf8");test("paid intervention proposals receive a separate pending entitlement",()=>{assert.match(sql,/entitlement_kind,offer_ref,status/);assert.match(sql,/'intervention'/);assert.match(sql,/'pending'/);assert.match(sql,/proposal_payload->>'commercialOfferRef'/);});test("existing paid interventions are relinked without changing POC entitlements",()=>{assert.match(sql,/SET entitlement_id=replacement\.id/);assert.match(sql,/current_entitlement\.entitlement_kind='diagnostic'/);assert.doesNotMatch(sql,/current_entitlement\.entitlement_kind='poc'/);});
