import test from"node:test";import assert from"node:assert/strict";import{readFile}from"node:fs/promises";
test("dual synthesis outputs are separately stored and tenant isolated",async()=>{const sql=await readFile(new URL("../db/migrations/043_diagnostic_dual_synthesis_outputs.sql",import.meta.url),"utf8");assert.match(sql,/CREATE TABLE app_shared\.diagnostic_executive_briefs/);assert.match(sql,/CREATE TABLE app_private\.diagnostic_revelation_guides/);assert.match(sql,/status IN \('locked','released'\)/);assert.equal((sql.match(/ENABLE ROW LEVEL SECURITY/g)||[]).length,2);assert.equal((sql.match(/FORCE ROW LEVEL SECURITY/g)||[]).length,2);assert.equal((sql.match(/FOREIGN KEY \(workspace_id,finding_id\)/g)||[]).length,2);assert.doesNotMatch(sql,/participant_slot|encrypted_response|deidentified_text/);});
