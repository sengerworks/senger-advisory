import test from "node:test";import assert from "node:assert/strict";import {readFile} from "node:fs/promises";const sql=await readFile(new URL("../db/migrations/022_commercial_checkout_foundation.sql",import.meta.url),"utf8");
test("checkout and webhook records are tenant isolated and entitlement linked",()=>{assert.match(sql,/commercial_checkout_sessions/);assert.match(sql,/commercial_webhook_events/);assert.match(sql,/REFERENCES app_shared\.commercial_entitlements/);assert.match(sql,/ENABLE ROW LEVEL SECURITY/g);assert.match(sql,/FORCE ROW LEVEL SECURITY/g);assert.match(sql,/current_workspace_id\(\)/);});
test("provider events are idempotent and card data has no storage field",()=>{assert.match(sql,/UNIQUE \(provider,provider_event_id\)/);assert.doesNotMatch(sql,/card_number|card_secret|payment_method_payload|billing_address/);});
