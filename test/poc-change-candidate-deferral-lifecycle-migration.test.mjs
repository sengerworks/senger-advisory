import test from "node:test";import assert from "node:assert/strict";import {readFile} from "node:fs/promises";
test("deferral preserves history without preventing a later terminal decision",async()=>{const migration=await readFile(new URL("../db/migrations/034_poc_change_candidate_deferral_lifecycle.sql",import.meta.url),"utf8");assert.match(migration,/DROP CONSTRAINT poc_change_candidate_decisions_workspace_id_candidate_id_key/);assert.match(migration,/UNIQUE INDEX poc_change_candidate_terminal_decision_idx/);assert.match(migration,/WHERE decision IN \('approved','rejected'\)/);assert.match(migration,/decision_history_idx/);});
