import test from "node:test";import assert from "node:assert/strict";import {readFile} from "node:fs/promises";
test("POC cohort decisions preserve tenant-isolated immutable product history",async()=>{const migration=await readFile(new URL("../db/migrations/031_poc_cohort_decisions.sql",import.meta.url),"utf8");assert.match(migration,/continue-with-adjustment/);assert.match(migration,/ready-to-monetize/);assert.match(migration,/required_changes jsonb/);assert.match(migration,/unresolved_risks jsonb/);assert.match(migration,/next_hypotheses jsonb/);assert.match(migration,/FORCE ROW LEVEL SECURITY/);assert.match(migration,/GRANT SELECT,INSERT/);assert.doesNotMatch(migration,/GRANT[^;]*(UPDATE|DELETE)/);});
