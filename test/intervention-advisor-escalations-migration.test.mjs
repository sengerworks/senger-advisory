import test from "node:test";import assert from "node:assert/strict";import {readFile} from "node:fs/promises";test("advisor escalation preserves tenant isolation and bounded support types",async()=>{const m=await readFile(new URL("../db/migrations/027_intervention_advisor_escalations.sql",import.meta.url),"utf8");assert.match(m,/facilitation','executive-coaching','learning-design','intervention-review/);assert.match(m,/UNIQUE INDEX intervention_advisor_escalation_open_idx/);assert.match(m,/FORCE ROW LEVEL SECURITY/);assert.doesNotMatch(m,/participant|email/i);});
