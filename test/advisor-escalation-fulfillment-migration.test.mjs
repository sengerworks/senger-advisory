import test from "node:test";import assert from "node:assert/strict";import {readFile} from "node:fs/promises";test("advisor escalation fulfillment records bounded handling",async()=>{const m=await readFile(new URL("../db/migrations/028_advisor_escalation_fulfillment.sql",import.meta.url),"utf8");assert.match(m,/advisor_note/);assert.match(m,/handled_by_clerk_user_id/);assert.match(m,/contacted_at/);assert.match(m,/closed_at/);});
