import test from "node:test";import assert from "node:assert/strict";import {readFile} from "node:fs/promises";test("POC feedback is identity-free and outside diagnostic evidence",async()=>{const m=await readFile(new URL("../db/migrations/029_poc_checkpoint_feedback.sql",import.meta.url),"utf8");assert.match(m,/poc_checkpoint_feedback/);assert.match(m,/participant-onboarding/);assert.match(m,/interview-completion/);assert.match(m,/purchase_intent='not-asked'/);assert.match(m,/FORCE ROW LEVEL SECURITY/);assert.doesNotMatch(m,/clerk_user|email|interview_id|evidence_id/i);});
