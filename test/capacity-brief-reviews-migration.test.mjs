import test from "node:test";import assert from "node:assert/strict";import {readFile} from "node:fs/promises";test("Brief intervention reviews are tenant-isolated and decision governed",async()=>{const migration=await readFile(new URL("../db/migrations/018_capacity_brief_reviews.sql",import.meta.url),"utf8");assert.match(migration,/capacity_brief_reviews/);assert.match(migration,/'continue','adjust','escalate','stop','reassess'/);assert.match(migration,/ENABLE ROW LEVEL SECURITY/);assert.match(migration,/FORCE ROW LEVEL SECURITY/);assert.doesNotMatch(migration,/participant|email|clerk_user/i);});

