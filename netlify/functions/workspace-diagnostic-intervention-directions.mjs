import { WORKSPACE_ROLES } from "../../workspace-authorization.js";
import { authenticateWorkspaceRequest } from "../lib/clerk-workspace-auth.mjs";
import { validateDiagnosticContextId } from "../lib/workspace-diagnostic-context.mjs";
import { DiagnosticEvidenceReviewAccessError } from "../lib/workspace-diagnostic-evidence-review.mjs";
import { createInterventionDirections, DiagnosticInterventionDirectionStateError, getInterventionDirections, interventionDirectionPolicy, reviewInterventionDirections } from "../lib/workspace-diagnostic-intervention-directions.mjs";
const headers={"Cache-Control":"no-store","Content-Type":"application/json; charset=utf-8","X-Content-Type-Options":"nosniff"},json=(status,body)=>new Response(JSON.stringify(body),{status,headers});
export function createWorkspaceDiagnosticInterventionDirectionsHandler({authenticate=authenticateWorkspaceRequest,get=getInterventionDirections,create=createInterventionDirections,review=reviewInterventionDirections}={}){return async request=>{if(!["GET","POST","PATCH"].includes(request.method))return json(405,{error:"Method not allowed."});const url=new URL(request.url),origin=request.headers.get("origin");if(origin&&origin!==url.origin)return json(403,{error:"Origin not allowed."});try{const auth=await authenticate(request);if(!auth.ok)return json(auth.status,{error:"Workspace access unavailable."});if(auth.value.role===WORKSPACE_ROLES.participant)return json(403,{error:"Participants cannot access Intervention Directions."});let diagnosticId;if(request.method==="GET")diagnosticId=validateDiagnosticContextId(url.searchParams.get("diagnosticId"));else{if(!request.headers.get("content-type")?.toLowerCase().startsWith("application/json"))return json(400,{error:"Use application/json."});const text=await request.text();if(text.length>1800)return json(400,{error:"Request too large."});let body;try{body=JSON.parse(text);}catch{return json(400,{error:"Submit a valid direction decision."});}if(request.method==="POST"&&Object.keys(body||{}).join(",")!=="diagnosticId")return json(400,{error:"Choose one diagnostic."});if(request.method==="PATCH"&&Object.keys(body||{}).sort().join(",")!=="decision,diagnosticId,reviewNote")return json(400,{error:"Submit one complete direction review."});diagnosticId=validateDiagnosticContextId(body.diagnosticId);if(request.method==="PATCH")return json(200,{directions:await review({workspaceId:auth.value.workspaceId,userId:auth.value.userId,input:{diagnosticId,decision:body.decision,reviewNote:body.reviewNote}}),policy:interventionDirectionPolicy});}const directions=request.method==="GET"?await get({workspaceId:auth.value.workspaceId,userId:auth.value.userId,diagnosticId}):await create({workspaceId:auth.value.workspaceId,userId:auth.value.userId,diagnosticId});return json(200,{directions,policy:interventionDirectionPolicy});}catch(error){if(error instanceof DiagnosticEvidenceReviewAccessError)return json(403,{error:"An active steward assignment is required."});if(error instanceof DiagnosticInterventionDirectionStateError)return json(409,{error:error.message});console.error("Intervention Directions failed",error instanceof Error?error.message:"Unknown error");return json(503,{error:"Intervention Directions are temporarily unavailable."});}};}
export default createWorkspaceDiagnosticInterventionDirectionsHandler();
export const config={path:"/api/workspace/diagnostic-intervention-directions",rateLimit:{windowLimit:20,windowSize:60,aggregateBy:["ip","domain"]}};
